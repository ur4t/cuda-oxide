# Must match the parent cuda-oxide toolchain exactly!
# rustc_private APIs change between nightly versions.
[toolchain]
channel = "1.96.0"
components = ["rust-src", "rustc-dev", "rust-analyzer", "clippy", "llvm-tools"]
